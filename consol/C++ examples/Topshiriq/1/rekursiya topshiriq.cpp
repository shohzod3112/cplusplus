#include <bits/stdc++.h>

using namespace std;

int recur (int s, int olma, int mandarin, int praynik)
{
    int n, n1, n2, soni = 0;
    n  = s / olma ;
    n1 = s / mandarin;
    n2 = s / praynik;

    if (n > 0 && n2 > 0 && n3 > 0)
    {
        for (int i = n - 1; i > 0; i--)
            if(olma * n + mandarin == s)
                soni ++;
            else
                if(olma * n + praynik == s)
                    soni ++;
                else

        if ()
            ;
    }

    else
        ++soni
}

int main ()
{


    return 0;
}
