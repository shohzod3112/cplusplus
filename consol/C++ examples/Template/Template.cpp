#include <bits/stdc++.h>

using namespace std;

template <typename T>

T sum(T a, T b)
{
    return a + b;
}
int main()
{

    int a, b;

    float c, d;

    cin >> a >> b;

    cout << sum(a, b);
    cin >> c >> d;

    cout << sum(c, d);
    return 0;
}
